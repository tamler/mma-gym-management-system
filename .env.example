# NextAuth Configuration
NEXTAUTH_SECRET=your-nextauth-secret-key-here-must-be-32-chars-minimum
NEXTAUTH_URL=http://localhost:3000

# Email Configuration (Zoho SMTP)
EMAIL_SERVER_HOST=smtp.zoho.com
EMAIL_SERVER_PORT=465
EMAIL_SERVER_USER=login@yourgym.com
EMAIL_SERVER_PASSWORD=your-zoho-password
EMAIL_FROM=login@yourgym.com

# Data Encryption (MUST be exactly 32 characters)
ENCRYPTION_KEY=your-32-character-encryption-key

# Emergency Admin Access (MUST be strong password, 12+ chars)
ADMIN_MASTER_PASSWORD=your-very-strong-master-password

# Gym Configuration
GYM_NAME="Elite MMA Training"
GYM_TIMEZONE="America/New_York"

# Notification System
CRON_SECRET=your-secure-cron-secret-key
ADMIN_SECRET=your-admin-secret-for-manual-testing

# Security Notes:
# 1. Change ALL default values before production
# 2. ENCRYPTION_KEY must be exactly 32 characters
# 3. ADMIN_MASTER_PASSWORD should be 12+ characters
# 4. Use HTTPS in production (NEXTAUTH_URL=https://yourdomain.com)
# 5. Never commit .env.local to version control